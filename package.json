{
  "name": "linkchat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm ci --prefix backend --omit=dev",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}